<?xml version="1.0" encoding="utf-8"?>
    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#FFFFFF"
        tools:context=".MVC.Admi_Activity">

    <include
        android:id="@+id/include2"
        layout="@layout/action_bar_toolbar"/>

    <!-- Banner superior (simple ImageView, sin Fresco) -->
    <ImageView
        android:id="@+id/ivBanner"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:adjustViewBounds="true"
        android:scaleType="fitXY"
        android:layout_marginTop="70dp"
        android:src="@drawable/tucanchaimgsplash"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Título -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Administrador"
        android:textColor="@color/black"
        android:textSize="22sp"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/ivBanner"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp" />


    <ImageView
        android:id="@+id/imgRoundLeft"
        android:layout_width="84dp"
        android:layout_height="84dp"
        android:src="@drawable/imgsplash"
        android:contentDescription="@string/app_name"
        android:scaleType="centerCrop"
        android:background="@drawable/circle_mask"
        android:padding="2dp"
        android:clipToOutline="true"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/imgRoundRight"
        app:layout_constraintHorizontal_chainStyle="packed"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="24dp" />

    <ImageView
        android:id="@+id/imgRoundRight"
        android:layout_width="84dp"
        android:layout_height="84dp"
        android:src="@drawable/imgsplash"
        android:contentDescription="@string/app_name"
        android:scaleType="centerCrop"
        android:background="@drawable/circle_mask"
        android:padding="2dp"
        android:clipToOutline="true"
        app:layout_constraintTop_toTopOf="@id/imgRoundLeft"
        app:layout_constraintStart_toEndOf="@id/imgRoundLeft"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Tarjeta contenedora -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardLogin"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:cardUseCompatPadding="true"
        app:cardCornerRadius="12dp"
        app:strokeWidth="1dp"
        app:strokeColor="@android:color/darker_gray"
        app:layout_constraintTop_toBottomOf="@id/imgRoundLeft"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"
        android:layout_marginHorizontal="8dp"
        android:paddingTop="16dp"
        android:paddingBottom="20dp"
        android:paddingHorizontal="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#FFFFFF"
            android:orientation="vertical">

            <!-- Botón Facebook -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnFacebook"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Registrar con Facebook"
                app:icon="@drawable/facebook"
                app:iconGravity="textStart"
                android:textColor="#000000"
                app:iconPadding="8dp"
                android:layout_marginTop="8dp" />

            <!-- Botón Google -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnGoogle"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Registrar con Google"
                app:icon="@drawable/google"
                app:iconGravity="textStart"
                android:textColor="#000000"
                app:iconPadding="8dp"
                android:layout_marginTop="12dp" />

            <!-- Separador con punto -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:gravity="center"
                android:orientation="horizontal"
                android:layout_marginTop="8dp">

                <View
                    android:layout_width="0dp"
                    android:layout_height="1dp"
                    android:layout_weight="1"
                    android:background="@android:color/darker_gray" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="•"
                    android:textSize="18sp"
                    android:paddingHorizontal="12dp"
                    android:textColor="@android:color/darker_gray" />

                <View
                    android:layout_width="0dp"
                    android:layout_height="1dp"
                    android:layout_weight="1"
                    android:background="@android:color/darker_gray" />
            </LinearLayout>

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

</androidx.constraintlayout.widget.ConstraintLayout>